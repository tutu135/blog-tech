---
title: BiLSTM-HGNN 架构设计思路
column: radar-gnn
pubDate: "2026-04-02"
summary: 双向 LSTM 捕捉时序特征,层次化图神经网络建模工作模式间的结构关系。
---

雷达工作模式识别任务里,时序特征和结构特征往往同样重要。BiLSTM 负责从信号序列里提取双向时序依赖,而 HGNN(层次化图神经网络)则把不同工作模式之间的层级关系建模成图结构,通过 DiffPool 做层次化的图池化。

两部分的输出在融合层拼接后送入分类头,最终在测试集上达到了 0.99 以上的 Macro F1。
